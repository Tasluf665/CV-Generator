const openai = require('../config/openai');

const analyzeAndSuggest = async (cvData, jobData) => {
  // Placeholder for complex OpenAI prompting
  // const response = await openai.chat.completions.create({...});
  return [];
};

module.exports = {
  analyzeAndSuggest,
};
